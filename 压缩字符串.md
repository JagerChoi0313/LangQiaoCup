压缩字符串

### 题目描述

实现一个算法来压缩一个字符串。压缩的要求如下：

1. 需要判断压缩能不能节省空间，仅在压缩后字符串比原字符串长度更短时进行压缩。
2. 压缩的格式是将连续相同字符替换为字符 + 数字形式，例如 "AAABCCDDDD" 变为 "A3BC2D4"。

### 输入描述

输入一行字符串，长度不超过 500.

### 输出描述

输出一行。若输入的字符串可压缩，则输出压缩后的字符串，否则输出 `NO`。

### 输入输出样例

#### 示例

> 输入

```txt
AAABCCDDDD
```

> 输出

```txt
A3BC2D4
```





下面我用**C++\**一步一步给你讲清楚这种“字符串压缩”的做法，并给出\**完整可直接运行的代码**。

------

## 一、题目要点整理

1. **连续相同字符** → 压缩为
   `字符 + 次数`（如果次数是 1，只输出字符，不加数字）
2. **只有当压缩后字符串更短**，才输出压缩结果
   否则输出 `NO`
3. 字符串长度 ≤ 500，直接遍历即可

------

## 二、解题思路

### 核心思路：双指针 / 单次遍历

- 从左到右遍历字符串
- 统计当前字符连续出现的次数
- 构造压缩字符串
- 最后比较长度

### 示例说明

输入：

```
AAABCCDDDD
```

遍历过程：

- A 连续 3 次 → `A3`
- B 连续 1 次 → `B`
- C 连续 2 次 → `C2`
- D 连续 4 次 → `D4`

结果：

```
A3BC2D4
```

------

## 三、C++ 实现代码

```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string s;
    cin >> s;

    string compressed = "";
    int n = s.length();

    for (int i = 0; i < n; ) {
        char current = s[i];
        int count = 1;

        // 统计连续相同字符
        while (i + count < n && s[i + count] == current) {
            count++;
        }

        // 加入压缩结果
        compressed += current;
        if (count > 1) {
            compressed += to_string(count);
        }

        i += count;
    }

    // 判断是否节省空间
    if (compressed.length() < s.length()) {
        cout << compressed << endl;
    } else {
        cout << "NO" << endl;
    }

    return 0;
}
```

------

## 四、复杂度分析

- **时间复杂度**：`O(n)`
- **空间复杂度**：`O(n)`
- 满足题目 `n ≤ 500` 的要求

------

## 五、再给你几个测试例子

| 输入         | 输出      |
| ------------ | --------- |
| `AAABCCDDDD` | `A3BC2D4` |
| `ABC`        | `NO`      |
| `AAAA`       | `A4`      |
| `AAB`        | `A2B`     |

------

如果你需要：

- **用 vector / 指针写法**
- **兼容空格输入**
- **面试版写法**

可以直接告诉我 👍







很好，这道题**非常适合理解“程序是怎么一步一步跑的”** 👍
我会 **结合示例输入 `AAABCCDDDD`，按执行顺序，逐行解释代码在干什么**，让你真正“看到”程序在跑。

------

## 一、完整代码（先整体看一眼）

```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string s;
    cin >> s;

    string compressed = "";
    int n = s.length();

    for (int i = 0; i < n; ) {
        char current = s[i];
        int count = 1;

        while (i + count < n && s[i + count] == current) {
            count++;
        }

        compressed += current;
        if (count > 1) {
            compressed += to_string(count);
        }

        i += count;
    }

    if (compressed.length() < s.length()) {
        cout << compressed << endl;
    } else {
        cout << "NO" << endl;
    }

    return 0;
}
```

------

## 二、逐行 + 执行过程讲解

### ① 头文件和命名空间

```cpp
#include <iostream>
#include <string>
using namespace std;
```

- `iostream`：用于 `cin / cout`
- `string`：使用 `string` 类型
- `using namespace std;`：不用写 `std::`

------

### ② 读取输入

```cpp
string s;
cin >> s;
```

**假设输入：**

```
AAABCCDDDD
```

此时：

```
s = "AAABCCDDDD"
```

------

### ③ 准备压缩字符串和长度

```cpp
string compressed = "";
int n = s.length();
```

- `compressed`：保存压缩结果
- `n = 10`（字符串长度）

------

## 三、核心逻辑（重点‼️）

### ④ for 循环（不写 i++）

```cpp
for (int i = 0; i < n; ) {
```

⚠️ **注意：这里没有 `i++`**

因为：

- `i` 一次要跳过一整段重复字符
- 在循环末尾用 `i += count`

------

## 四、结合执行过程详细讲解

### 第一次循环

#### 当前状态

```
i = 0
s[i] = 'A'
char current = s[i];
int count = 1;
```

- `current = 'A'`
- `count = 1`

------

### while 循环：统计连续字符

```cpp
while (i + count < n && s[i + count] == current) {
    count++;
}
```

#### 执行过程

| 检查位置 | 字符 | 是否相同 | count |
| -------- | ---- | -------- | ----- |
| s[1]     | A    | 是       | 2     |
| s[2]     | A    | 是       | 3     |
| s[3]     | B    | 否       | 停止  |

最终：

```
count = 3
```

------

### 生成压缩结果

```cpp
compressed += current;   // 加 'A'
compressed = "A"
if (count > 1) {
    compressed += to_string(count);
}
compressed = "A3"
```

------

### 移动 i

```cpp
i += count;
i = 0 + 3 = 3
```

------

## 五、后续循环简略展示

### 第二次循环

```
i = 3 → 'B'
count = 1
```

- 不加数字

```
compressed = "A3B"
i = 4
```

------

### 第三次循环

```
i = 4 → 'C'
count = 2
compressed = "A3BC2"
i = 6
```

------

### 第四次循环

```
i = 6 → 'D'
count = 4
compressed = "A3BC2D4"
i = 10 → 循环结束
```

------

## 六、最终判断是否压缩成功

```cpp
if (compressed.length() < s.length()) {
    cout << compressed << endl;
} else {
    cout << "NO" << endl;
}
```

比较长度：

```
原字符串长度：10
压缩后长度：7
```

满足条件 → 输出：

```
A3BC2D4
```

------

## 七、关键思想总结（面试必会）

### 🔑 1. 为什么 `for` 里没有 `i++`？

因为：

```cpp
i += count;
```

一次跳过一整段重复字符，效率更高

------

### 🔑 2. 为什么 count 初始为 1？

因为：

- 当前字符本身就算 1 次

------

### 🔑 3. 为什么 count = 1 不加数字？

题目要求：

```
A → A
AA → A2
```

------

如果你愿意，下一步我可以帮你：

- ✏️ 手写模拟一遍
- 🔁 改成不用 while 的写法
- 🧠 讲清楚「双指针思想」
- 🧪 帮你出几道相似练习

直接说你想继续哪一步就行 👌