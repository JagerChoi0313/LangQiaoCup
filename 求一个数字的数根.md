求一个数字的数根

### 题目描述

实现一个算法求一个数字的树根。介绍如下：

- 将一正整数 N 的各个位数相加(即横向相加)后，若加完后的值大于等于 10 的话，则继续将各位数进行横向相加直到其值小于 10 为止所得到的数，即为数根。
- 例如对于数字 138，有 1+3+8=12，1+2=3，则 138 的数根为 3。

### 输入描述

输入一行，为正整数 N (0≤N≤105)*N* (0≤*N*≤105)。

### 输出描述

输出一行，为数字 N*N* 的数根。

### 输入输出样例

#### 示例

> 输入

```txt
138
```

> 输出

```txt
3
```









## ✅ 一、你现在的思路分析

你说的流程是：

1. 把数字拆成数组
2. 转成字符串
3. 相加
4. 再转成数字
5. 判断是否 ≥10
6. 如果是继续重复

👉 这样确实能做出来
 👉 但其实根本 **不用数组，也不用频繁类型转换**

因为：

- 如果输入是字符串，本来就可以直接遍历
- 如果输入是整数，可以用 `%10` 和 `/10` 取每一位

------

# ✅ 二、最简单的思路（推荐）

核心逻辑：

```
只要当前数字 >= 10
    把各位数相加
    得到新数字
```

------

# ✅ 三、方法一：用整数处理（推荐写法）

### 思路

用 `%10` 取个位
 用 `/10` 去掉个位

### 代码

```
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    while (n >= 10) {      // 只要 >=10 就继续
        int sum = 0;
        while (n > 0) {
            sum += n % 10; // 取个位
            n /= 10;       // 去掉个位
        }
        n = sum;           // 更新 n
    }

    cout << n << endl;
    return 0;
}
```

------

# ✅ 四、方法二：用字符串（更直观）

如果你怕 `%10` 忘记，可以用字符串：

```
#include <iostream>
using namespace std;

int main() {
    string n;
    cin >> n;

    while (n.length() > 1) {
        int sum = 0;
        for (int i = 0; i < n.length(); i++) {
            sum += n[i] - '0';  // 字符转数字
        }
        n = to_string(sum);     // 再转回字符串
    }

    cout << n << endl;
    return 0;
}
```

------

# ✅ 五、进阶：数学公式（高手写法）

其实数根有公式：

```
如果 n == 0 -> 数根是 0
否则 数根 = (n - 1) % 9 + 1
```

例如：

138
 (138 - 1) % 9 + 1
 = 137 % 9 + 1
 = 2 + 1
 = 3

代码：

```
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    if (n == 0)
        cout << 0;
    else
        cout << (n - 1) % 9 + 1;

    return 0;
}
```

------

# ✅ 六、总结对比

| 方法          | 推荐度 | 难度     |
| ------------- | ------ | -------- |
| `%10` / `/10` | ⭐⭐⭐⭐⭐  | 最基础   |
| 字符串        | ⭐⭐⭐    | 简单直观 |
| 数学公式      | ⭐⭐⭐⭐   | 最优雅   |

------

# 🎯 建议

你现在是：

- C++语法有点生疏
- 想找最清晰思路

👉 建议你写 `%10` 那个版本
 👉 这是算法入门的基本功

------

如果你愿意，我可以：

- 给你画一个“数字拆分流程图”
- 或者出 3 道类似题帮你巩固
- 或者带你复习一遍 C++ 常用语法

你想走哪条路线 😄

给我看看方法一的执行过程

好 👍 我们就用 **方法一（%10 + /10）**，拿题目示例 `138` 来完整走一遍执行过程。

------

# 🌟 代码回顾

```
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    while (n >= 10) {
        int sum = 0;
        while (n > 0) {
            sum += n % 10;
            n /= 10;
        }
        n = sum;
    }

    cout << n << endl;
    return 0;
}
```

------

# 🧠 输入

```
138
```

------

# 🔄 第一轮外层 while（n >= 10）

此时：

```
n = 138
```

因为 138 >= 10，进入循环。

------

## 🔹 内层 while 开始

### 第 1 次循环

```
n = 138
sum = 0
```

计算：

```
n % 10 = 8   （取个位）
sum = 0 + 8 = 8
n = 138 / 10 = 13
```

------

### 第 2 次循环

```
n = 13
sum = 8
```

计算：

```
n % 10 = 3
sum = 8 + 3 = 11
n = 13 / 10 = 1
```

------

### 第 3 次循环

```
n = 1
sum = 11
```

计算：

```
n % 10 = 1
sum = 11 + 1 = 12
n = 1 / 10 = 0
```

------

### 内层 while 结束

因为：

```
n = 0
```

跳出内层循环。

此时：

```
sum = 12
```

执行：

```
n = sum
```

现在：

```
n = 12
```

------

# 🔄 第二轮外层 while（n >= 10）

因为：

```
n = 12 >= 10
```

继续进入。

------

## 🔹 内层 while

### 第 1 次

```
n = 12
sum = 0
n % 10 = 2
sum = 2
n = 1
```

------

### 第 2 次

```
n % 10 = 1
sum = 3
n = 0
```

------

内层结束：

```
n = sum = 3
```

------

# 🔄 第三轮外层 while

```
n = 3
```

因为：

```
3 < 10
```

外层 while 结束。

------

# 🎯 最终输出

```
3
```

------

# 🧩 整个过程用表格看更清晰

| 轮次  | n 初始值 | 每次取出的数 | sum  | 结果  |
| ----- | -------- | ------------ | ---- | ----- |
| 第1轮 | 138      | 8,3,1        | 12   | n=12  |
| 第2轮 | 12       | 2,1          | 3    | n=3   |
| 结束  | 3        | -            | -    | 输出3 |

------

# 🔥 关键理解点

## 1️⃣ n % 10 是干嘛？

👉 取最后一位

------

## 2️⃣ n /= 10 是干嘛？

👉 去掉最后一位

------

## 3️⃣ 为什么用两个 while？

外层：控制“是否还需要继续求和”
 内层：负责“拆分当前数字”

------

如果你愿意，我可以：

- 给你画一个「数字分解动态图示」
- 或者出一道变式题让你自己走流程
- 或者帮你总结 `%10` 和 `/10` 的核心技巧

你想再深入一点吗 😄